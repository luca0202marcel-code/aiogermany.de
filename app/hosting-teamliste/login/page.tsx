'use client'
import { FormEvent, useState } from 'react'
import { useRouter } from 'next/navigation'
export default function HostingLogin() { const [password,setPassword]=useState(''); const [error,setError]=useState(''); const router=useRouter(); async function submit(e:FormEvent){e.preventDefault();setError('');const r=await fetch('/api/hosting/login',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({password})});if(r.ok) router.push('/hosting-teamliste'); else setError('Zugang verweigert.')} return <main className="admin-shell"><div className="admin-panel auth-panel"><p className="eyebrow">ALL IN ONE · PRIVATE HOSTING</p><h1>Hosting Zugang</h1><p className="muted">Private Bot-Infrastruktur für die Teamliste.</p><form onSubmit={submit} className="admin-form"><label>Hosting-Passwort<input autoFocus type="password" value={password} onChange={e=>setPassword(e.target.value)} required /></label>{error&&<p className="form-error">{error}</p>}<button className="neon-button" type="submit">Konsole öffnen</button><a className="ghost-button" href="/">Zur Startseite</a></form></div></main> }
