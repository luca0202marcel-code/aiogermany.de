'use client'
import { useState } from 'react'
import { Activity, Bot, Clock3, RefreshCw, Server, Terminal } from 'lucide-react'
export default function HostingConsole(){const [status,setStatus]=useState('ONLINE');const [busy,setBusy]=useState(false);const [logs,setLogs]=useState(['[system] Hosting-Konsole initialisiert','[discord] Worker wartet auf Gateway-Verbindung','[sync] Rollen-Allowlist geladen']) ;async function action(command:string){setBusy(true);await new Promise(r=>setTimeout(r,500));setStatus(command==='stop'?'OFFLINE':'ONLINE');setLogs(l=>[...l,`[control] ${command.toUpperCase()} ausgeführt`]);setBusy(false)} return <main className="hosting-shell"><header className="hosting-header"><div><p className="eyebrow">ALL IN ONE · PRIVATE INFRASTRUCTURE</p><h1>Teamliste Hosting</h1><p className="muted">Discord Worker Control Plane</p></div><a href="/" className="ghost-button">Website</a></header><section className="hosting-grid"><article className="hosting-status"><div className="status-pulse"/><div><p className="eyebrow">WORKER STATUS</p><strong>{status}</strong><p className="muted">Gateway · Teamliste Worker</p></div></article><article className="hosting-metric"><Server size={18}/><span><small>UPTIME</small><b>12d 04h 18m</b></span></article><article className="hosting-metric"><Activity size={18}/><span><small>LAST SYNC</small><b>vor 2 Minuten</b></span></article><article className="hosting-metric"><Bot size={18}/><span><small>COMMANDS</small><b>/teamrole-add · /teamrole-remove</b></span></article></section><section className="hosting-panel"><div className="hosting-panel-head"><div><p className="eyebrow">PROCESS CONTROL</p><h2>Worker Steuerung</h2></div><div className="hosting-actions"><button className="neon-button" disabled={busy} onClick={()=>action('start')}>Start</button><button className="ghost-button" disabled={busy} onClick={()=>action('restart')}><RefreshCw size={15}/> Restart</button><button className="ghost-button danger-button" disabled={busy} onClick={()=>action('stop')}>Stop</button></div></div><div className="hosting-terminal"><div className="terminal-bar"><Terminal size={15}/> worker.log <span>LIVE</span></div>{logs.map((log,i)=><p key={i}>{log}</p>)}</div></section><section className="hosting-panel"><p className="eyebrow">ROLE POLICY</p><h2>Teamrollen-Allowlist</h2><p className="muted">25 freigegebene Discord-Rollen · Verwaltung nur durch autorisierte Teamrollen im Zielkanal.</p><div className="role-policy"><span>ADD / REMOVE</span><b>1543209684482396250</b><b>1543209687804158014</b><span>CHANNEL</span><b>1543209907040297051</b></div></section><footer className="hosting-footer"><Clock3 size={14}/> Worker-Secrets bleiben serverseitig · Steuerung protokolliert</footer></main>}
